
# Only produce additional .md file if asked or necessary; for documentary purpose .md, place them under docs folder, if no such folder exists in the working dirctory, create one.
# Ensure every file in docs/ uses a numeric prefix for ordering (e.g., 01_title.md).
# Avoid running training commands on your own, provide command and have me run it

# Prefer modular solutions over monolithic functions